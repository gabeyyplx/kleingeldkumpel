<form method="post" action="{{ path('app_recurring_transaction_delete', {'id': recurring_transaction.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recurring_transaction.id) }}">
    <button class="btn">Delete</button>
</form>
